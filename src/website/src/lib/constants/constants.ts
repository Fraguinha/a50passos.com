import path from 'path'

const numberItems = 9
const paginationNumber = 5

const imageSizeSmall = 1080
const imageSizeBig = 1350

const maxFileSize = 1000 * 1000 * 10

const ROOM = 1
const APARTMENT = 2

export {
  numberItems,
  paginationNumber,
  imageSizeSmall,
  imageSizeBig,
  maxFileSize,
  ROOM,
  APARTMENT,
}
